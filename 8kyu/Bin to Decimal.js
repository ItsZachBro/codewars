function binToDec(bin) {
    return parseInt(bin, 2)
}
// parseInt() function is used to convert the binary string to decimal
// the number 2 as the second argument is used to indicate that the input is a binary number